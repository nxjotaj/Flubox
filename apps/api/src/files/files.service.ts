import{BadRequestException,ForbiddenException,Injectable,NotFoundException,OnModuleInit}from'@nestjs/common';import{ConfigService}from'@nestjs/config';import{createClient,SupabaseClient}from'@supabase/supabase-js';import{createHash,randomUUID}from'crypto';import{FileKind}from'@prisma/client';import{PrismaService}from'../prisma/prisma.service';
@Injectable()export class FilesService implements OnModuleInit{private sb:SupabaseClient;private bucket='flubox-private';constructor(private db:PrismaService,c:ConfigService){this.sb=createClient(c.getOrThrow('SUPABASE_URL'),c.getOrThrow('SUPABASE_SECRET_KEY'),{auth:{persistSession:false}})}async onModuleInit(){const{x}= {x:await this.sb.storage.getBucket(this.bucket)};if(x.error)await this.sb.storage.createBucket(this.bucket,{public:false,fileSizeLimit:10*1024*1024,allowedMimeTypes:['application/pdf','image/jpeg','image/png','image/webp']})}async upload(orderId:string,kind:FileKind,file:Express.Multer.File,user:any){if(!['PAYMENT_RECEIPT','SHIPPING_LABEL','INVOICE','CONTENT_DECLARATION'].includes(kind))throw new BadRequestException('Tipo de documento inválido');if(!file||!['application/pdf','image/jpeg','image/png','image/webp'].includes(file.mimetype)||file.size>10*1024*1024)throw new BadRequestException('Arquivo inválido. Use PDF, JPG, PNG ou WEBP até 10 MB');const order=await this.db.order.findUnique({where:{id:orderId},include:{payments:true,files:true}});if(!order)throw new NotFoundException('Pedido não encontrado');if(user.role==='SELLER'&&order.sellerId!==user.id)throw new ForbiddenException();const key=`orders/${orderId}/${kind.toLowerCase()}-${randomUUID()}-${file.originalname.replace(/[^a-zA-Z0-9._-]/g,'_')}`;const up=await this.sb.storage.from(this.bucket).upload(key,file.buffer,{contentType:file.mimetype,upsert:false});if(up.error)throw new BadRequestException('Falha no armazenamento: '+up.error.message);const saved=await this.db.fileObject.create({data:{ownerId:user.id,orderId,kind,storageKey:key,filename:file.originalname,contentType:file.mimetype,sizeBytes:file.size,checksum:createHash('sha256').update(file.buffer).digest('hex'),scanStatus:'accepted'}});const all=[...order.files,saved];if(order.payments.some(p=>p.status==='CONFIRMED')&&all.some(f=>f.kind==='SHIPPING_LABEL')&&all.some(f=>['INVOICE','CONTENT_DECLARATION'].includes(f.kind)))await this.db.order.update({where:{id:orderId},data:{status:'PAID'}});return saved}async url(id:string,user:any){const f=await this.db.fileObject.findUnique({where:{id},include:{order:true}});if(!f)throw new NotFoundException('Arquivo não encontrado');if(user.role==='SELLER'&&f.order?.sellerId!==user.id)throw new ForbiddenException();const x=await this.sb.storage.from(this.bucket).createSignedUrl(f.storageKey,300);if(x.error)throw new BadRequestException(x.error.message);return{url:x.data.signedUrl,expiresIn:300}}}
